--- a/pytest.ini
+++ b/pytest.ini
@@ -16,6 +16,7 @@ addopts =
 markers =
     smoke: marks critical smoke tests (105 tests, GitHub CI tier, ~30s-2min)
     slow: marks tests as slow (deselect with '-m "not slow"')
+    comprehensive: marks comprehensive validation tests (all parametrizations, production MCMC)
     gpu: marks tests that require GPU acceleration
     integration: marks integration tests
     unit: marks unit tests
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -132,6 +132,7 @@ markers = [
     "benchmark: performance benchmarks",

     # Execution characteristics
     "slow: marks tests as slow (> 30 seconds each, deselect with '-m \"not slow\"')",
+    "comprehensive: comprehensive validation with all parametrizations (60-90 min)",
     "gpu: marks tests that require GPU",

     # Notebook tests
