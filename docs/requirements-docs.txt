# Documentation Build Requirements
# ================================
# Install with: pip install -r docs/requirements-docs.txt
#
# Matches local uv environment versions for consistency

# Core Sphinx and theme (exact versions from uv.lock)
sphinx==8.2.3
sphinx-rtd-theme==3.0.2
sphinx-autodoc-typehints==3.5.2

# Sphinx contrib packages (exact versions from uv.lock)
sphinxcontrib-applehelp==2.0.0
sphinxcontrib-devhelp==2.0.0
sphinxcontrib-htmlhelp==2.1.0
sphinxcontrib-jsmath==1.0.1
sphinxcontrib-qthelp==2.0.0
sphinxcontrib-serializinghtml==2.0.0

# Optional Sphinx extensions (minimum versions for compatibility)
sphinx-copybutton>=0.5.0      # Copy button for code blocks
myst-parser>=4.0.0            # Markdown support

# Required for autodoc to parse JAX code (exact versions match codebase)
jax==0.8.0
jaxlib==0.8.0
nlsq>=0.2.1

# Required for intersphinx and examples
numpyro>=0.19.0,<1.0.0
arviz>=0.22.0,<1.0.0
