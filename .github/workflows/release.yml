name: Release to PyPI

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    - name: Install uv
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
      with:
        enable-cache: true
    - name: Set up Python
      run: uv python install 3.12
    - name: Install dependencies
      run: uv sync --frozen
    # Kept in sync with the identical lint job in ci.yml — see comment there.
    - name: Run Ruff format check
      run: uv run ruff format --check rheojax tests
    - name: Run Ruff
      run: uv run ruff check rheojax tests

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    - name: Install uv
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
      with:
        enable-cache: true
    - name: Set up Python
      run: uv python install 3.12
    - name: Install PySide6 system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libegl1 libopengl0 libxkbcommon-x11-0 libxcb-xinerama0 libxcb-cursor0
    - name: Install dependencies
      run: uv sync --frozen
    - name: Run smoke tests
      shell: bash
      env:
        QT_QPA_PLATFORM: offscreen
      run: |
        uv run pytest -n 4 -v --timeout=120 \
          -m "smoke"

  publish:
    name: Publish to PyPI
    needs: [lint, test]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: pypi
      url: https://pypi.org/p/rheojax
    permissions:
      id-token: write
      contents: write
      attestations: write

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        fetch-depth: 0

    - name: Install uv
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
      with:
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.12

    - name: Verify version matches release tag
      env:
        GIT_REF: ${{ github.ref }}
      run: |
        PACKAGE_VERSION=$(uv run python -c "
        try:
            import tomllib
        except ImportError:
            import tomli as tomllib
        with open('pyproject.toml', 'rb') as f:
            print(tomllib.load(f)['project']['version'])
        ")
        TAG_VERSION="${GIT_REF#refs/tags/v}"
        echo "Package version: $PACKAGE_VERSION"
        echo "Git tag version: $TAG_VERSION"
        if [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
          echo "::error::Package version ($PACKAGE_VERSION) does not match git tag ($TAG_VERSION)"
          exit 1
        fi

    - name: Build package
      run: uv build

    - name: Verify package metadata
      run: uvx twine check dist/*

    - name: Generate build provenance attestation
      uses: actions/attest-build-provenance@8beda2b7ed98355c0e97c0a63bec38ae472e66c4 # v4
      with:
        subject-path: 'dist/*'

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1 (v1.14.2)
      with:
        print-hash: true
        verbose: true
        attestations: true

    - name: Generate SBOM
      continue-on-error: true
      run: |
        uv sync --frozen
        uvx cyclonedx-py environment -o sbom.json --output-format json

    - name: Generate changelog from commits
      run: |
        PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
        if [ -z "$PREV_TAG" ]; then
          git log --pretty=format:"- %s (%h)" --no-merges HEAD > changelog.md
        else
          git log --pretty=format:"- %s (%h)" --no-merges "${PREV_TAG}..HEAD" > changelog.md
        fi

    - name: Create GitHub Release
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        TAG_NAME: ${{ github.ref_name }}
      run: |
        ASSETS=(dist/*)
        [ -f sbom.json ] && ASSETS+=(sbom.json)
        gh release create "$TAG_NAME" \
          --title "RheoJAX $TAG_NAME" \
          --notes-file changelog.md \
          "${ASSETS[@]}"

    - name: Upload SBOM artifact
      if: hashFiles('sbom.json') != ''
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
      with:
        name: sbom
        path: sbom.json
