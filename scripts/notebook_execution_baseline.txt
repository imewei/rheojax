# Baseline of notebooks with the silent-skip pattern (fit/quality cell
# unexecuted while the rest of the notebook ran). Snapshot taken 2026-05-18
# after the STZ NB04/NB05 fix. Listed paths are skipped by the QA scanner
# in scripts/check_notebook_execution.py.
#
# Goal: shrink this list to zero over time. Whenever you fix one of these
# notebooks (re-execute cleanly), delete the corresponding line so the
# scanner re-protects it from regression.
#
# A NEW notebook landing in this file constitutes a regression in the
# pre-commit gate / CI test (tests/test_notebook_execution_consistency.py).
#
# Lines starting with '#' and blank lines are ignored. Paths are relative
# to the repo root.
#
# Updated 2026-07-10: root-caused and fixed 33 of the 34 previously-listed
# notebooks (itt_mct Prony-decomposition cold-start perf bug, dmt/sgr/vlb/tnt/
# hvm/bayesian/verification stale-baseline prunes, ikh 04/10 stale-baseline
# prunes). One entry remains genuinely blocked, see below.

# Blocked by a missing proprietary dataset, not a code bug: the FAST_MODE
# guard around fit_bayesian() was added (matching the sibling single-mode
# FIKH notebook's convention -- the unguarded NUTS call took ~28.6h in the
# analogous single-mode case per NB02's committed timing), but cell 5's
# load_pnas_startup() call fails before ever reaching it, because
# examples/data/ikh/PNAS_DigitalRheometerTwin_Dataset.xlsx is intentionally
# .gitignore'd (commit ac5b589) and absent from every checkout. See
# examples/data/README.md "Missing Proprietary Dataset" for the full list of
# notebooks this affects and the VLB synthetic-fallback precedent that could
# resolve it. Cannot be re-executed/verified until that data-availability gap
# is closed.
examples/fikh/08_fmlikh_startup_shear.ipynb
